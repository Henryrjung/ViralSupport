<nav class="topnav">
    <a class="active" href="/">Home</a>
    <a href="/news">News</a>
    <a href="/contact">Contact</a>
    <a href="/about">About</a>
    <a href="/chat">Blog</a>
    <a href="/profile" style="float: right">Profile</a>
    <a href="/login" style="float: right">Login</a>
    <a href="/signup" style="float: right">Sign Up</a>
</nav>

<h2 class="has-text-weight-bold">Posts</h2>

<div class="">
    <div class="">
        <div class="">
        </div>
        <div class="">
            <a class="button is-primary" href="/post">New Post</a>
        </div>
    </div>
    <div class="">
        <div class="">
        </div>
    </div>
</div>

<div>
    <ul>
        {{#each posts}}  
        <li>
            <h2>{{this.title}}</h2>
            <p>{{this.body}}</p>
        </li>
        {{/each}}
    </ul>
    

</div>


<!-- Custom Script -->
<script src="js/blog.js" type="text/javascript"></

<script>
 $('form').submit(function(userName){
        if ($('#input-message').val() != '') {
            // own send message handlebar render
            var source = $('#handlebars-own-send-msg').html();
            var template = Handlebars.compile(source);
            var context = { 'text' : $('#input-message').val()};
            var theCompiledHtml = template(context);
            $('#messages').append($(theCompiledHtml).hide().fadeIn(1000));
            scrollToBottom();
            // broadcast to another user
            socket.emit('chat message',{
                    userinfo: userinfo,
                    inputval: $('#input-message').val()
            });
            $('#input-message').val('');
        }
        return false;
    });
 
 <script src="../../socket.io/socket.io.js"></script>
    <script src="http://code.jquery.com/jquery-1.10.1.min.js"></script>